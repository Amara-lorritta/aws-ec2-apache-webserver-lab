# aws-ec2-apache-webserver-lab
Hands-on AWS lab that demonstrates how to recreate a default VPC, launch an EC2 instance, configure security groups, install Apache, and publish a custom web page. Includes SSH access, web server configuration, and validation steps.
